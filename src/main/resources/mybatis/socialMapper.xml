<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.web.mapper.SocialMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="org.example.web.entity.Post">
        <id column="id" property="id" />
        <result column="content" property="content" />
        <result column="images" property="images" />
        <result column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="avatar" property="avatar" />
        <result column="is_anonymous" property="isAnonymous" />
        <result column="category" property="category" />
        <result column="status" property="status" />
        <result column="like_count" property="likeCount" />
        <result column="comment_count" property="commentCount" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, content, images, user_id, username, avatar, is_anonymous,
        category, status, like_count, comment_count, created_at, updated_at
    </sql>

    <!-- 根据 ID 查询帖子 -->
    <select id="getById" parameterType="long" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List" />
        FROM post
        WHERE id = #{id}
          AND status = 1
    </select>

    <!-- 查询帖子列表（分页） -->
    <select id="getList" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List" />
        FROM post
        WHERE status = 1
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="isAnonymous != null">
            AND is_anonymous = #{isAnonymous}
        </if>
        <if test="category != null">
            AND category = #{category}
        </if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 查询帖子总数 -->
    <select id="getCount" parameterType="java.util.Map" resultType="int">
        SELECT COUNT(*)
        FROM post
        WHERE status = 1
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="isAnonymous != null">
            AND is_anonymous = #{isAnonymous}
        </if>
        <if test="category != null">
            AND category = #{category}
        </if>
    </select>

    <!-- 根据用户 ID 查询帖子列表 -->
    <select id="getByUserId" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List" />
        FROM post
        WHERE user_id = #{userId}
          AND status = 1
        ORDER BY created_at DESC
    </select>

    <!-- 插入帖子（待审核状态） -->
    <insert id="insert" parameterType="org.example.web.entity.Post" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post (
            content, images, user_id, username, avatar, is_anonymous, category,
            status, like_count, comment_count, created_at, updated_at
        ) VALUES (
            #{content}, #{images}, #{userId}, #{username}, #{avatar}, #{isAnonymous}, #{category},
            1, 0, 0, NOW(), NOW()
        )
    </insert>

    <!-- 更新帖子 -->
    <update id="update" parameterType="org.example.web.entity.Post">
        UPDATE post
        <set>
            <if test="content != null">content = #{content},</if>
            <if test="images != null">images = #{images},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="isAnonymous != null">is_anonymous = #{isAnonymous},</if>
            <if test="category != null">category = #{category},</if>
            <if test="status != null">status = #{status},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="commentCount != null">comment_count = #{commentCount},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除帖子（软删除，更新 status） -->
    <update id="deleteById" parameterType="long">
        UPDATE post
        SET status = 0,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 审核通过帖子 -->
    <update id="approvePost" parameterType="long">
        UPDATE post
        SET status = 1,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 审核拒绝/屏蔽帖子 -->
    <update id="rejectPost" parameterType="long">
        UPDATE post
        SET status = 2,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 查询待审核帖子列表 -->
    <select id="getPendingList" parameterType="java.util.Map" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List" />
        FROM post
        WHERE status = 2
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 查询待审核帖子总数 -->
    <select id="getPendingCount" resultType="int">
        SELECT COUNT(*)
        FROM post
        WHERE status = 2
    </select>

    <!-- 更新点赞数 -->
    <update id="updateLikeCount">
        UPDATE post
        SET like_count = like_count + (#{delta}),
        updated_at = NOW()
        WHERE id = #{postId}
        AND status = 1
        AND like_count + (#{delta}) >= 0
        <!-- 如果结果 < 0，WHERE 条件不成立，这条 SQL 不会执行 -->
    </update>

    <!-- 更新评论数 -->
    <update id="updateCommentCount" parameterType="java.util.Map">
        UPDATE post
        SET comment_count = comment_count + #{delta},
            updated_at = NOW()
        WHERE id = #{postId}
          AND status = 1
    </update>

    <!-- 批量查询帖子（用于热搜等） -->
    <select id="getBatch" parameterType="java.util.List" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List" />
        FROM post
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND status = 1
        ORDER BY created_at DESC
    </select>

</mapper>
